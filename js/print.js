// Funciones para impresión de ficha
document.addEventListener('DOMContentLoaded', function() {
    // window.print();
});

// Cerrar la ventana después de imprimir (opcional)
window.onafterprint = function() {
    // setTimeout(() => window.close(), 1000);
};

